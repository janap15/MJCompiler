// provera svih simbola

program MJProgram
	int x, y; 
{
	void main() {
		true, false
		program break class enum else const if do while new print read return void extends continue foreach
		ident 1509 true 'j'
		+ - * / % == != > >= < <= && || = ++ -- ; : , . ( ) [ ] { } =>
		x--; // ovo je komentar, x = x - 1;
		@#$ f "
	}
}